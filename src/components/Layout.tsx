import Head from "next/head";
import Sidebar from "./Sidebar";

const Layout = ({children}: any) => {
  return (
    <>
      <Head>
        <title>Shortify - ln</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className="w-100 flex flex-col items-center min-h-screen">
        <h1 className="mt-5 text-5xl md:text-[5rem] leading-normal font-extrabold text-gray-700 mx-auto">
          Shortify - <span className="text-purple-300">ln</span>
        </h1>

        <div className="w-screen md:p-9 p-3 flex flex-1 flex-row">
          <Sidebar />

          <div className="mx-9 w-full flex justify-center">
            {children}
          </div>

          <aside className="md:w-96 flex flex-col align-bottom items-center justify-end" aria-label="Sidebar"></aside>
        </div>

        {/* { session.data && <UserData user={session.data.user}/> } */}
      </main>
    </>
  );
};

export default Layout;